// Requiring our db model... but we'll worry about this later.
// var db = require("../models");


console.log("the api-routes file is connected");

// Routes
// =============================================================
module.exports = function(app) {

};